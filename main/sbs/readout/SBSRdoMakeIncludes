#
#  Establish the compilers
#

CC=gcc
CXX=/usr/opt/mpi/openmpi-4.0.1/bin/mpicxx -std=c++11
CXXLD=/usr/opt/mpi/openmpi-4.0.1/bin/mpicxx
CCLD=gcc

COMMONFLAGS= -g -I. -I/usr/opt/daq/11.4-028/include -I/usr/opt/daq/11.4-028/include/sbsreadout \
	     -DHAVE_VME_MAPPING -DHAVE_SBSVME_INTERFACE \
	      -I/usr/include/tcl8.6 -I/usr/include/tcl8.6 -pthread

CXXFLAGS = $(COMMONFLAGS)  $(USERCXXFLAGS)

CCFLAGS= $(COMMONFLAGS) $(USERCCFLAGS)

# Note THREAD_LDFLAGS are left off because:
#  1. We need a threaded build of Tcl and the TCL_LDFLAGS get those.
#  2. Both include -lieee which has a symbol that's not as weak as it should
#     be and results in double definitions if it appears twice on the link line.
#

LDFLAGS=-L/usr/opt/daq/11.4-028/lib -lSBSProductionReadout -lSBSDeviceSupport -lSBSVmeAPI \
		       -lException -ldaqthreads -ldaqshm -ltclPlus  \
		       $(USERLDFLAGS) \
		        -L/usr/lib/x86_64-linux-gnu -ltcl8.6 -ldl -lz  -lpthread -lm  -Wl,"-rpath=/usr/opt/daq/11.4-028/lib"
